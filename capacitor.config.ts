import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.rofelixk.grimorio',
  appName: 'grimorio',
  webDir: 'dist/grimorio/browser'
};

export default config;
