import { Component } from '@angular/core';
import { RouterLink } from '@angular/router';

@Component({
  selector: 'app-decks',
  imports: [RouterLink],
  templateUrl: './decks.html',
})
export class Decks {}
